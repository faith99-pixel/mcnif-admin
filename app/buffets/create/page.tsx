import React from 'react'
import CreateBuffetPage from './CreateBuffetPage'

type Props = {}

const CreateBuffet = (props: Props) => {
    return (
        <CreateBuffetPage />
    )
}

export default CreateBuffet